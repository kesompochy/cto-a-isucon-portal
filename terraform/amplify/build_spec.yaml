version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - yarn
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*