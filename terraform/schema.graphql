type TeamScore {
  team_id: ID!
  pass: Boolean!
  score: Int!
  success: Int!
  fail: Int!
  messages: [String!]
  timestamp: Int!
}

type Query {
  getAllScores: [TeamScore]! @aws_cognito_user_pools
}

type Mutation {
  updateTeamScore(team_id: ID!, pass: Boolean!, score: Int!, success: Int!, fail: Int!, messages: [String!], timestamp: Int!): TeamScore @aws_api_key
}